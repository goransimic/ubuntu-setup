# Ubuntu Setup

Setup Ubuntu system

## Usage

```sh
curl -s https://raw.githubusercontent.com/goransimic/ubuntu-setup/master/setup.sh | bash -s -- all|core|cli|desktop|system|gestures|zsh|zellij|docker|mise|lazygit|lazydocker|alacritty
```
